# 半拍 0.4 验证记录

2026-09-22 · macOS 本地 Node.js v25.9.0 与 Codex 内置浏览器。

## 示范强弱与来源力度（后续增量）

- `npm test`：125 / 125 通过。93 个源 MIDI 的 58,072 个音符逐事件验证音高、起音、时值、手别、力度和速度；30 个条目有力度变化，63 个为固定力度。克列门蒂三个乐章全部为 velocity 90，10 个入门编配不添加虚构力度。
- 新测试覆盖力度校验、JSON / MIDI 往返、叠层轨道去重、变速与分段保留力度、暂停续播，以及力度不改变原有评分和练习证据指纹。
- `tests/dynamics-browser.html` 使用正式 `PianoSound` 输出图及 30 个本地采样，在 OfflineAudioContext 检查三种音色 × C3 / C4 / C6 × 35 / 80 / 115 三档力度及均匀对照。9 组全部通过：强弱输出 RMS 相差约 7.85–9.29 dB，均匀对照的三档输出一致，所测单音没有削波。此为声音信号验证，不代表主观音乐表达或真实琴触键验收。可运行 `node scripts/serve-qa.mjs` 后打开 `http://localhost:3001/dynamics-qa-local.html` 复测。
- 主应用实际操作：三级《坦诉 Op.100 No.1》显示已有强弱信息，示范至 00:16 可暂停并继续；切换均匀对照会停止旧示范，再播放仍从 00:00 / 00:36 开始。克列门蒂第一乐章显示固定力度，旧版入门旋律显示缺少力度。查看了 1280 × 720 的谱面与力度控件截图。
- 全曲库浏览器回归：103 个条目正常渲染，无异常、告警或小节降级；播放柱偏差小于 0.001 CSS px，双行预读与采样检查通过。强弱只来自已有 velocity，没有自动添加原谱表情符号、踏板或自由速度。
- `npm run repertoire:build`、`npm run build`、模块语法与 `git diff --check` 通过；未调用真实 DeepSeek API，未再次连接实体钢琴。

## 三级新增克列门蒂三个乐章（后续增量）

- 按用户确认加入 Op.36 No.1 的 I · Spiritoso、II · Andante、III · Vivace，分别为 38 / 26 / 70 小节，333 / 332 / 473 个音符。三级共 13 个条目，全库 103 个；与 Git 初版数据逐条比较，原有 90 个来源转录条目完全未变。
- `npm test`：116 / 116 通过；全部 93 个来源 MIDI 逐事件比对音高、起音、时值、左右手与速度。新增测试锁定三个乐章的压缩包文件对应关系、开头旋律、全段长度、调号、拍号与速度单位，并确认第二乐章装饰音没有丢失。
- 浏览器全曲库检查：103 个条目全部渲染，无异常、告警或小节降级；原有播放柱几何、双行预读和 30 个钢琴采样解码检查仍通过。
- 主应用：三级筛选显示 13，搜索“克列门蒂”显示三个乐章，均可打开完整谱面并开始示范；第一乐章播放约 46 秒后可暂停，第二乐章播放至约 44 秒，第三乐章以 3/8、四分音符 BPM 80 进入播放。页面实际截图已查看，未再次连接真实琴键。
- `npm run repertoire:build`、`npm run build`、构建脚本语法检查及 `git diff --check` 通过。版本、单遍反复和源谱记谱边界见 [SOURCES.md](./SOURCES.md)。

## 多行看谱与提前预读（后续增量）

- `npm test`：115 / 115 通过，新增 4 项跟谱边界测试，覆盖下一行预读、放大后优先当前行、倒退和曲尾。
- 浏览器以新默认 80% 逐首渲染全部 100 首完整曲谱，无异常、告警或渲染降级。50% / 80% / 180% 左右手游标检查的最大中心偏差小于 0.001 CSS px；30 个采样正常解码。
- 独立 1000 × 500 谱面测试：八分音符双手谱每行 4 小节，完整显示 2 行；跳到第 2、3、5 行以及回到第 1 行时，当前行与下一行均完整可见。关闭跟谱不滚动，重新启用恢复跟随，换行不滚动外层页面；340px 谱面容器内当前行完整可见。
- 主应用 1280 × 720：肖邦 Op.25 No.2 默认 80% 显示两行、每行两小节；224 BPM 示范跨行播放至约 00:30，暂停后仍能同时看到第 29–30 与 31–32 小节。暂停时缩至 70% 和切换专注模式，时间与游标保留；专注模式每行可显示 4 小节（具体数量依曲目密度而变）。
- 缩放设为 70% 后刷新仍为 70%，点击「默认」恢复 80%。主应用 390 × 844 无整页横向溢出，谱面排在连接说明之前，专注模式顶部控制按钮可见且能正常退出。结束检查后恢复桌面尺寸、普通模式和 80%。
- `npm run build`、语法检查和 `git diff --check` 通过。本次验证为浏览器播放与排版检查，未再次连接真实钢琴；未重跑全部历史外部 Playwright 脚本。

## 示范暂停与继续（后续增量）

- `npm test`：111 / 111 通过。新增测试覆盖长音采样偏移、暂停取消排队音符、长时间停留不计入播放时钟、连续暂停/继续、休止与和弦、停止重播、换曲取消旧回调、自然结束与选段边界。
- 主应用浏览器：播放至 00:06 后暂停，跨工具操作停留后时间与 SVG 游标完全一致、琴键高亮清空；继续后从 00:06 接着播放。暂停时放大到 110% 仍有原位置游标。
- 停止后按钮、播放时间和游标重置；再次播放从 00:00 开始。暂停时改为第 2 小节起奏会清除旧位置，新示范从第 2 小节开始，总时长变为 9 秒。
- 暂停/继续、独立停止按钮与时间的实际页面布局已查看。`npm run build` 与 `git diff --check` 通过。

## 0.4 曲库与 AI 验证

- `npm test`：103 / 103 通过。保留原有分析、MIDI、输入与存储测试；新增 7/30 天摘要、等长前周期、重复/未来/模拟排除、实琴与屏幕分组、提前结束、私密笔记排除、DeepSeek 固定请求与错误处理、本机代理来源限制、中文多字节分块、十级进度备份兼容。
- 100 个分级条目每级恰好 10 个，ID 不重复；纯基本功练习不计入。90 首源 MIDI 与生成曲库逐事件比对音高、起音、时值、手别和变速，全部一致。新一级编配单独标明主题／节选与重新编配范围。
- 浏览器用真实 VexFlow 逐首渲染全部 100 首完整曲谱，未出现渲染降级、异常或告警。此检查验证渲染和数据完整性，不等同于逐页校订原版记谱。
- 播放柱几何检查：左右手 × 70% / 100% / 180% 缩放 × 从第 2 小节开始，柱中心与符头中心最大偏差约 0.00074 CSS px。另在主应用点示范检查实际坐标，中心吻合；检查了肖邦 Op.25 No.2 的实际排版与三连音。
- 30 个本地 MP3 全部经浏览器 AudioContext 解码，采样发声、复音、松键和温暖音色路径执行成功。音色喜好与真实钢琴搭配效果尚需用户试听。
- 主应用：分级曲库显示 100，一级/十级筛选各显示 10；十级作品能进入练习；路线有 10 个等级。AI 页面缺 Key 与无个人记录均有明确提示。
- 独立 `localhost:3001` 模拟服务：预览 30 天摘要 → 手动生成 → 生成中切换到别页 → 返回打开报告 → 刷新后报告仍在。仅用合成补记与假 Key，本轮**未调用真实 DeepSeek**，真实响应及账户余额需填 Key 后验证。
- `npm run build` 成功；构建只含正式静态资源，`.env`、服务器和测试页面不进入 dist。`git diff --check` 通过；`.env` 被 Git 忽略。

本轮未重跑下面历史记录中的全部外部 Playwright 脚本；它们的曲库与等级数量断言已更新。用户报告过初版实琴流程通过，本次新增功能没有再次连接真实硬件验证。

## 本轮复跑

```bash
npm test
npm run repertoire:build
npm run build
node scripts/serve-qa.mjs
```

最后一条命令仅启动独立的本机模拟服务，不读取 `.env`，不调用 DeepSeek。在浏览器打开 `http://localhost:3001/qa-local.html` 点击全库检查；`http://localhost:3001/coach-qa-local.html` 用于报告交互与持久化验证。报告样例数据只存于此测试来源，与端口 3000 的日常练习分开。完成后 Ctrl+C 停止测试服务。

# 0.3 历史验证记录

日期：2026-09-22。下列原有自动化验证在 Linux Chromium 与 Node.js 中运行，未连接实体钢琴；本机复跑与用户实琴反馈另列如下。

## 本地初版验收（2026-09-22）

- macOS 本地 Node.js v25.9.0：复跑 92 项单元测试全部通过，静态构建成功。
- 本地浏览器：页面和五线谱正常加载，模拟练习完成并生成错漏音及起音节奏反馈。
- 用户反馈：已连接实际钢琴测试，练习流程没有问题。琴型、浏览器版本与输入延迟未记录，此结论仅覆盖用户本次实测环境。
- 用户发现的待改进项：演奏位置游标与音符未对齐；曲库与学习路线需扩展至业余 10 级；计划新增 DeepSeek 阶段点评；改善合成音色。本次初版保留当前功能，后续分别实现和验证。

## 0.3 已通过

- 92 项单元测试：旧版单音、双手和弦、乱序与展开起音、错漏多音、长谱性能、设备状态竞争、MIDI 0/1 与坏文件、JSON 校验、变速/分段/休止及五线谱准备。
- `tests/browser.cjs`：9 首谱库与分类；599/Hanon 谱面、缩放、专注、分手/分小节；模拟输入到反馈/历史；电脑键盘事件、提前结束、取消倒数、空录制；图形新增/拖动/方向键/指法/左右手/休止/撤销重做/保存刷新；JSON 粘贴错误和示例、MIDI 轨道选择、备份恢复、错误 MIDI、下载；自动循环两遍；模拟 Web MIDI 授权、按键、力度零抬键、设备断开与不支持浏览器提示；390px 界面无整页横向溢出。无页面 JavaScript 异常。
- `tests/editor-browser.cjs`：复制跨小节长音扩展曲终；导入 7/8 与 F#m 的显示/保存一致；旧版有效记录迁移一次、损坏项不崩溃；清空历史支持取消且旧记录不复活。
- `tests/notation-browser.cjs`：双谱表、和弦、附点、延音、极端/空谱、599 双高音谱号、音符点击；自动跟谱只滚动谱面内部；窄屏布局。
- 导入模块独立验证：轨道名称保留高音区左手；超过 2 MB 的合法备份可导入，64 MB 上限拦截；批量保存失败只重试剩余部分，取消停止后续保存。
- 源码示例 JSON / MIDI 往返保持两小节、9 个发声音符与左右手；JSON 另外保留休止及指法。

## 0.3 新增验证

- `tests/progress.test.mjs` 新增 16 项：60 项学习清单；本人/老师依据与待复核分离；实琴/虚拟摘要及模拟/空录制排除；午夜开始与旧记录默认选项；闰年、周界、跨年、连续天数；音符加权准确率、额外音分母、补记/提前结束无伪成绩；重复/未来记录；指纹及跨 ID 计划证据；最近三次、两天、阈值/版本/范围/声部/速度/时区排序；严格备份校验与大于 32 MB 的中文笔记往返。
- `tests/progress-browser.cjs`：全新数据空态、只读示例、周目标与目标等级、补记新增/修改/删除、老师反馈不能为空、每级 12 项及进度持久化、纸谱/关联曲目计划和直接打开跟练；备份下载/取消恢复/重复恢复/非法备份零写入；真实虚拟键盘采集的原子摘要、模拟排除；1024/768/390px 概览/路线/计划布局；v1 数据库中 105 遍先归档再保留最近 100 遍，刷新不重复；清空保留设置和曲谱；存储损坏时明确提示且禁用完整备份；同步克隆失败时多仓库事务全部回滚。无页面 JavaScript 异常。
- 本轮重新运行 `tests/browser.cjs`，练习、反馈、导入、编辑、模拟 Web MIDI 和窄屏流程全部通过。
- 截图核验覆盖成长空态、明确标注的示例、个人记录、等级路线、手机后备布局以及练习室。主要说明文字增加字号并加深颜色。

## 原有 Playwright 脚本复跑

应用与单元测试无需安装第三方 npm 依赖：

```bash
npm test
npm start
npm run build
```

浏览器验收需自行安装 Playwright 及 Chromium（这些仅用于测试，不属于应用运行依赖）：

```bash
npm install --no-save playwright
npx playwright install chromium
node tests/browser.cjs
node tests/progress-browser.cjs
node tests/editor-browser.cjs
node tests/notation-browser.cjs
```

也可以通过 `QA_CHROMIUM` 指向现有 Chromium 可执行文件。测试自启并关闭本地服务器，在全新浏览器上下文使用合成数据，不会读取线上个人练习。

## 尚待验证与产品边界

用户已反馈其本机实琴流程通过；其他琴型、数据线、驱动、USB 枚举、浏览器权限以及实琴输入延迟仍需分别实测。模拟 Web MIDI 不等于硬件验收。

0.3 使用本地规则分析音高和起音，当时未接入云端 AI 或音频识别。自由速度、宽琶音、持续时值、踏板和音乐表现力不在本版评分范围。MIDI 五线谱为重建排版，复杂复调的原始分声部与表情不会完整恢复。个人数据只在当前浏览器保存，源码包不含个人练习数据。
